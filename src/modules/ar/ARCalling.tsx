import './ARDashboard.css'

/** AI Calling — automated payer follow-up. Reserved section (blank, ready for
 *  the calling workflow build-out). */
function ARCalling() {
  return <section className="ard-panel" aria-label="AI Calling" />
}

export default ARCalling
